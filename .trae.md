# 九层塔 · 项目规则（TraeWork 助手必读）

## 部署

- **默认部署到 GitHub Pages**，不要部署到 IGA Pages 或其他平台
- 部署命令：`npm run build` 然后 `npx gh-pages -d dist`
- 线上地址：https://mhdfy1988.github.io/jiuchongta/
- 仓库地址：https://github.com/mhdfy1988/jiuchongta

## 提交代码

- 使用 Conventional Commits 规范：`type(scope): description`
- 提交前确保 `npm test` 全部通过
- 重要改动同步更新 README.md

## 代码规范

- 框架：Vue 3 + Vite，Composition API
- 状态管理通过 `src/systems/` 下的各系统模块 + EventBus 解耦
- 组件放在 `src/components/`，游戏逻辑放 `src/systems/`，不要混写
- 测试用 Vitest + @vue/test-utils，放在 `tests/` 目录

## 已确认的设计决策

- 计分动画：双击才跳过（单击无效），避免误触
- 左侧当前分在计分动画结束后才滚动上涨
- Boss "不许重复"：能打但重复牌型不计分（不是直接拦截）
- Boss "断色"：被禁花色的牌不参与牌型判定和计分
- 游戏结束结算只显示本局新解锁的成就
